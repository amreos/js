$(".modal").remove();
$('#flash').remove();
$("header#banner").after('<div id="flash"><div id="notice">Jobs Sorted</div></div>');

$(".job_section").html("<%= escape_javascript(render :partial => 'shared/index/job') %>")


var flashMargin = ($(window).width() - $("#flash").width()) / 2; 
$("#flash").css({'left' : flashMargin+"px"});
$("#flash").delay(5000).fadeOut('slow', function() {$(this).remove();});