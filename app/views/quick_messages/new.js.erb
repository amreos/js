$(".modal").append("<%= escape_javascript(render(:partial => 'form', :locals => {:note => @note})) %>").addClass("closeAfterSuccess");

$("#flash").remove();

$(".modal").dialog({
 width: 600,
 title: "Send a Quick Message",
 position: ['center',0]
});

$("a").blur();
