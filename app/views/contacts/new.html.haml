.form
 
  %p.object_links
    = link_to "Back to #{@client.name}", client_path(@client), :class => "button"
  
  %h1= "Add a Contact"
  
  = form_for([@client, @contact]) do |f|
    = render :partial => 'form', :locals => { :f => f, :contact => @contact, :client => @client }
  
    .actions
      = f.submit "Add Contact", :class => "button", "data-disable-with" => "Saving . . ."
