# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  date:
    formats:
      # Use the strftime parameters for formats.
      # When no format has been given, it uses default.
      # You can provide other formats here if you like!
      default: "%Y-%m-%d"
      short: "%b %d"
      long: "%B %d, %Y"
  time:
    formats:
      default: "%Y-%m-%d"
      short: "%b %d"
      long: "%B %d, %Y"
      calendar: "%B %d, %Y"
      note_archive: "%m/%d/%y at %l:%M%p"
  errors:
    applicants:
      candidate_id: "Candidate is already placed or flagged"
      state: "A Candidate is alredy placed on this job"          
    messages:
      invalid_date: "is not a valid date"
      invalid_time: "is not a valid time"
      invalid_datetime: "is not a valid datetime"
      is_at: "must be at %{restriction}"
      before: "must be before %{restriction}"
      on_or_before: "must be on or before %{restriction}"
      after: "must be after %{restriction}"
      on_or_after: "must be on or after %{restriction}"
      wrong_size: "is the wrong size (should be %{file_size})"
      size_too_small: "is to small (should be at least %{file_size})"
      size_too_big: "is to big (should be at most %{file_size})"
  mongoid:
    attributes:
      question:
        inquirer: "Your Name"
        inquirer_email: "Your Email"
        content: "Your Question"
      job:
        job_number: "Legacy Job Number"
